user   root root;

events {

}

http {
  server {
    listen 80 default_server;
    root {{root_dir}};

    location / {
        index index.html;
    }

    location = /favicon.ico {
        log_not_found off;
    }

    error_log /var/log/nginx/error.log debug;

  }
}
